<%= render partial: 'flash_message', locals: {message: 'The order was successfully destroyed.', timeout: 3500} %>
var row = document.querySelector("tr[data-order-id='<%= @order_id %>'")
row.parentNode.removeChild(row);
