<%= simple_form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :status, collection: Order.statuses, label_method: :first, value_method: :first, include_blank: false %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
